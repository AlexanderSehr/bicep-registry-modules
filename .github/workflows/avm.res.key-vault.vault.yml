name: "avm.res.key-vault.vault"

on:
  schedule:
    - cron: "0 12 1/15 * *" # Bi-Weekly Test (on 1st & 15th of month)
  workflow_dispatch:
    inputs:
      staticValidation:
        type: boolean
        description: "Execute static validation"
        required: false
        default: true
      deploymentValidation:
        type: boolean
        description: "Execute deployment validation"
        required: false
        default: true
      removeDeployment:
        type: boolean
        description: "Remove deployed module"
        required: false
        default: true

  push:
    branches:
      # - main
      - users/alsehr/gitHubEvent
    paths:
      - ".github/actions/templates/avm-**"
      - ".github/workflows/avm.template.module.yml"
      - ".github/workflows/avm.res.key-vault.vault.yml"
      - "avm/res/key-vault/vault/**"
      - "avm/utilities/pipelines/**"

  pull_request:
    branches:
      # - main
      - users/alsehr/gitHubEvent
    paths:
      - ".github/actions/templates/avm-**"
      - ".github/workflows/avm.template.module.yml"
      - ".github/workflows/avm.res.key-vault.vault.yml"
      - "avm/res/key-vault/vault/**"
      - "avm/utilities/pipelines/**"

env:
  modulePath: "avm/res/key-vault/vault"
  workflowPath: ".github/workflows/avm.res.key-vault.vault.yml"

concurrency:
  group: ${{ github.workflow }}

jobs:
  ###########################
  #   Initialize pipeline   #
  ###########################
  job_initialize_pipeline:
    runs-on: ubuntu-20.04
    name: "Initialize pipeline"
    steps:
      - name: "Checkout"
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: "Get (calculated) workflow controls"
        id: get-workflow-controls
        uses: ./.github/actions/templates/avm-getWorkflowControls
        with:
          workflowPath: "${{ env.workflowPath}}"
          modulePath: "${{ env.modulePath }}"
      - name: "Get module test file paths"
        id: get-module-test-file-paths
        uses: ./.github/actions/templates/avm-getModuleTestFiles
        with:
          modulePath: "${{ env.modulePath }}"
    outputs:
      moduleTestFilePaths: ${{ steps.get-module-test-file-paths.outputs.moduleTestFilePaths }}
      calculatedActions: ${{ steps.get-workflow-controls.outputs.calculatedActions }}
      psRuleModuleTestFilePaths: ${{ steps.get-module-test-file-paths.outputs.psRuleModuleTestFilePaths }}
      modulePath: "${{ env.modulePath }}"

  ##############################
  #   Call reusable workflow   #
  ##############################
  call-workflow-passing-data:
    name: "Run"
    needs:
      - job_initialize_pipeline
    uses: ./.github/workflows/avm.template.module.yml
    with:
      moduleTestFilePaths: "${{ needs.job_initialize_pipeline.outputs.moduleTestFilePaths }}"
      psRuleModuleTestFilePaths: "${{ needs.job_initialize_pipeline.outputs.psRuleModuleTestFilePaths }}"
      modulePath: "${{ needs.job_initialize_pipeline.outputs.modulePath}}"
      calculatedActions: "${{ needs.job_initialize_pipeline.outputs.calculatedActions }}"
    secrets: inherit
